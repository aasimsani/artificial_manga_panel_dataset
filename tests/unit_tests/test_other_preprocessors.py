# Convert images
    # Convert a set of sample images
    # Check if they were output in L format

# Extract and verify fonts
    # extract fonts - throw a zip file see if it extracts
    # get font files - put some files in a folder see if it moves them
    # make char list
    # create character test string
    # Verify font files
    # Check if it spits out the right set of 
    # files given a set of fonts