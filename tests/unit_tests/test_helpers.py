# Crop image outside
    # Test whether image without black area gets shrunk
    # Test if image with black area gets shrunk right
    # Test if image which is all black throws error
# Get min area panel
    # Check if it gets the right panel by name
# Get leaf panels
    # Check if it gets the right num of panel
    # Try a few cases
# Find parent with multiple children
    # Check if it finds all the panels
    # with children
# Move child to line
    # Check if it returns the right
    # movement amount
        # H and V
# Move children to line
    # Check if children are moved
    # to the right line
        # H and V
